'use strict';

describe('myApp.AddJob module', function() {

  beforeEach(module('myApp.AddJob'));

  describe('AddJob controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var AddJobCtrl = $controller('AddJobCtrl');
      expect(AddJobCtrl).toBeDefined();
    }));

  });
});